import { Path } from "./types";

const paths: Path[] = [
  // { name: "Dashboard", path: "/dashboard" },
  { name: "Earn", path: "/" },
  { name: "Stake", path: "/stake" },
  { name: "Vote", path: "/vote" },
];

export default paths;
