import { Path } from "./types";

const paths: Path[] = [
  // { name: "Dashboard", path: "/" },
  { name: "Earn", path: "/" },
  // { name: "Stake", path: "/stake" },
  // { name: "Vote", path: "/vote" },
  // { name: "Claim", path: "/claim" },
];

export default paths;
